<Window x:Class="OfficeHVAC.Applications.RoomSimulator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OfficeHVAC.Applications.RoomSimulator"
        xmlns:regions="http://prismlibrary.com/"
        xmlns:views="clr-namespace:OfficeHVAC.Modules.RoomSimulator.Views;assembly=OfficeHVAC.Modules.RoomSimulator"
        mc:Ignorable="d"
        Title="MainWindow" Height="500" Width="750">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        
        <ContentControl regions:RegionManager.RegionName="ServerLogRegion" Grid.RowSpan="2"/>

        <views:RoomSimulator Grid.Column="1" Grid.Row="0" />
        <views:RoomSimulator Grid.Column="1" Grid.Row="1" />

        <ContentControl regions:RegionManager.RegionName="TimeSimulationRegion" Grid.Row="2" Grid.ColumnSpan="2" />

    </Grid>
</Window>
