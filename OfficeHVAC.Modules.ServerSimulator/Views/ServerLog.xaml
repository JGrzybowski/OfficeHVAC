<UserControl x:Class="OfficeHVAC.Modules.ServerSimulator.Views.ServerLog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mvvm="http://prismlibrary.com/"
             mvvm:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        
        <ScrollViewer x:Name="LogScroller">
            <ListView ItemsSource="{Binding Logs}" x:Name="LogList" />
        </ScrollViewer>

        <Button Width="75" Height="30" Grid.Row="1"
                Content="Initialise" Command="{Binding InitializeCommand}" />
    </Grid>
</UserControl>
